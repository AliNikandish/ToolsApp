type ToolsCard={
    color:string,
    title:string,
    desc:string,
    to:string,
    icon:React.ReactNode
  }

  type CategoryCard={
    color:string,
    category:string,
    to:string,
    Icon:React.ReactNode,
    count:number
  }

  type HeadingProps={
    title:string
  }

  type childrenProp={
    children: React.ReactNode;
  }